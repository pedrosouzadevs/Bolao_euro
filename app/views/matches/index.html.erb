<div class="flex items-center justify-center" >
  <div class="flex w-2/3 min-h-full flex-col justify-center mx-6 my-8 lg:mx-8 shadow-md sm:rounded-lg">
    <table class="divide-y  divide-gray-200 text-sm rtl:text-right text-gray-500 dark:text-gray-400">
      <tbody >
        <% @matches.each do |match| %>
          <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-600">
           <th>
              <p class="px-6 py-1 text-center font-small break-all text-gray-400 max-w-1/3">
                <%= match.team_a.name %>
              </p>
              <p class="px-6  flex justify-center py-1 text-center font-small break-all text-gray-400 max-w-1/3">
                <%= render 'shared/flags_team_a', team: match.team_a %>
              </p>
            </th>
            <th>
              <p class="px-6 py-1 text-center font-small break-all text-gray-400 max-w-1/4">
                <% result = Result.find_by(match_id: match.id) %>
                <% if result.present? && result.score_team_a.present? && result.score_team_b.present? %>
                  <%= "#{result.score_team_a} X #{result.score_team_b}" %>
                <% else %>
                  <%= "Not played yet" %>
                <% end %>
              </p>
            </th>
            <th>
              <p class="px-6 py-1 text-center font-small break-all text-gray-400 max-w-1/3">
                <%= match.team_b.name %>
              </p>
              <p class="px-6  flex justify-center py-1 text-center font-small break-all text-gray-400 max-w-1/3">
                <%= render 'shared/flags_team_a', team: match.team_b %>
              </p>
            </th>
            <th>
              <%= link_to new_match_bet_path(match.id) do %>
                <?xml version="1.0" ?><!DOCTYPE svg  PUBLIC '-//W3C//DTD SVG 1.1//EN'  'http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd'><svg height="20px" id="Layer_1" style="enable-background:new 0 0 512 512;" version="1.1" viewBox="0 0 512 512" width="20px" xml:space="preserve" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"><polygon points="160,115.4 180.7,96 352,256 180.7,416 160,396.7 310.5,256 "/></svg>
              <% end %>
            </th>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
